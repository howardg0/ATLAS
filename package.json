{
  "name": "atlas-training-log",
  "version": "6.4.0",
  "private": true,
  "description": "ATLAS — offline-first strength training log (static PWA, no build step)",
  "scripts": {
    "test": "node --test tests/core.test.js tests/data.test.js tests/version.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
